#!/bin/bash
usage() {
    echo "Usage: $0 [PATH] "
    exit
}    

check_uid() {
    if [[ $(id -u) -ne 0 ]]; then
        echo "Must be ran as root"
        exit
    fi
}

check_uid

if [[ $# -ne 1 ]]; then
    usage
fi

find "$1" -iname '.*' 
