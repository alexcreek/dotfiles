#!/bin/bash
usage() {
    echo "Usage: $0 [PORT]"
    exit
}    

check_uid() {
    if [[ $(id -u) -ne 0 ]]; then
        echo "Must be ran as root"
        exit
    fi
}

check_uid

if [[ $# -ne 1 ]]; then
    usage
fi

watch -n1 "lsof -i tcp:$1"
