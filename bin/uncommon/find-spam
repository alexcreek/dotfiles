#!/bin/bash
usage() {
    echo "Usage: $0 "
    exit
}    

check_uid() {
    if [[ $(id -u) -ne 0 ]]; then
        echo "Must be ran as root"
        exit
    fi
}

check_uid

#if [[ $# -ne 1 ]]; then
#    usage
#fi

grep -ri 'eval' /var/spool/exim4/ | cut -f 4 -d ':' | sort | uniq 
