#!/bin/bash
usage() {
    echo "Usage: $0 [FILENAME] '[COMMENT CHARACTER]'"
    exit
}    

check_uid() {
    if [[ $(id -u) -ne 0 ]]; then
        echo "Must be ran as root"
        exit
    fi
}

check_uid

if [[ $# -ne 2 ]]; then
    usage
fi

grep -v "$2" "$1" | egrep -v "^$" 
