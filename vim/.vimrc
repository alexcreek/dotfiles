""" General UI
" Not sure what this does
syntax enable

" highlight matching brackets
set showmatch

" hide search results highlighting
set nohlsearch

" show line numbers
"set number


""" Tabs/Spaces
" switch TAB key to use spaces instead of tabs 
set expandtab 

" make TAB key insert 4 spaces
set tabstop=4

" make BACKSPACE remove 4 spaces 
set softtabstop=4

" set auto indent to 4 spaces
set shiftwidth=4

" enable auto indents on newlines
"set autoindent


""" Groking
" enable case insensitive matching
set ignorecase 
